feat(dashboard-orders): masquer ID, filtre période personnalisé, retrait stat Livraison

Interface commandes (dashboard admin)
- Ne plus afficher l'ID de la commande : vue carte (titre = client ou Table X), vue liste (colonne ID supprimée), modal détail (titre "Détails de la commande", description = date/heure)
- Filtre période : intervalle choisi par l'utilisateur (champs Du / Au type date), bouton Effacer ; plus de select prédéfinis (Aujourd'hui, Cette semaine, etc.)
- Retrait de la carte stat "Livraison" ; grille stats en 4 colonnes (Total, Actives, Sur place, À emporter)

Fichiers modifiés
- src/app/dashboard/orders/page.tsx : OrderCard titre, periodStart/periodEnd, periodRange useMemo, inputs date Du/Au, suppression carte Livraison
- src/components/tables/order-columns.tsx : suppression colonne ID
- src/components/modals/OrderDetailsModal.tsx : title + description sans order.id
